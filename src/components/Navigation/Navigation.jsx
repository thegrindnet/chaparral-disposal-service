import { useState } from 'react';import { navLinks } from '../../utils/constants';import './Navigation.css';export default function Navigation(){const[open,setOpen]=useState(false);return <nav className="nav" aria-label="Main navigation"><button className="nav__toggle" aria-expanded={open} aria-controls="main-menu" onClick={()=>setOpen(!open)}><span></span><span></span><span></span><span className="sr-only">Menu</span></button><ul className={`nav__list ${open?'nav__list--open':''}`} id="main-menu">{navLinks.map(({label,href})=><li key={href}><a className="nav__link" href={href} onClick={()=>setOpen(false)}>{label}</a></li>)}</ul></nav>}
