import {galleryItems} from '../../utils/constants';import './Gallery.css';export default function Gallery(){return <section className="gallery" id="gallery"><div className="container"><p className="eyebrow">Our equipment</p><div className="gallery__heading"><h2 className="section-title">Built for the route.<br/>Ready for the work.</h2><p className="section-copy">Real trucks and containers from the local team serving your neighborhood.</p></div><div className="gallery__grid">{galleryItems.map((item,index)=><figure className={`gallery__item gallery__item--${index+1}`} key={item.src}><img src={item.src} alt={item.alt} loading="lazy"/><figcaption>{item.label}</figcaption></figure>)}</div></div></section>}
